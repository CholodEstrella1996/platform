import { FormModalComponent } from './formModal.component'
import { FormModalProps } from './formModal.model'

export const FormModalContainer = (props: FormModalProps) => <FormModalComponent {...props} />
