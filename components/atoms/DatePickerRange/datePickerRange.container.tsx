import { DatePickerRangeComponent } from './datePickerRange.component'
import { DateRangePickerProps } from './datePickerRange.model'

export const DatePickerRangeContainer = (props: DateRangePickerProps) => (
  <DatePickerRangeComponent {...props} />
)
