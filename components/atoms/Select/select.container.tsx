import SelectComponent from './select.component'
import { SelectProps } from './select.models'

const SelectContainer = (props: SelectProps) => <SelectComponent {...props} />

export default SelectContainer
