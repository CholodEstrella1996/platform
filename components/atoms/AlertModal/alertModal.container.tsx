import { AlertModalComponent } from './alertModal.component'
import { AlertModalProps } from './alertModal.model'

const AlertModal = (props: AlertModalProps) => <AlertModalComponent {...props} />

export default AlertModal
