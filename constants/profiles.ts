import messages from 'components/modules/Invites/invite.messages'

const PROFILES = {
  student: messages.invite.profiles.student,
  teacher: messages.invite.profiles.teacher,
  director: messages.invite.profiles.director,
  children: messages.invite.profiles.myChildren,
  manager: messages.invite.profiles.manager,
}

export { PROFILES }
